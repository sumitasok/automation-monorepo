# Private pack

Your personal automations. This directory stays in your private workspace repo
and is **never** shared. Add jobs with `auto new` and choose pack `private`.

If a private job later becomes generally useful, move its folder into
`packs/shared/jobs/` and set `visibility: shared` to contribute it back.
