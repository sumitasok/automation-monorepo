# Track binary / large data with git-lfs so the repo stays fast to clone.
# Run once per machine: git lfs install
#
# NOTE: SQLite state DBs are NOT tracked here — they are machine-local run data
# and are git-ignored (see .gitignore and docs/adr/0005). Only intentionally
# shared binary ASSETS (a reference PDF/image shipped inside a pack) use LFS.
*.xlsx                filter=lfs diff=lfs merge=lfs -text
*.pdf                 filter=lfs diff=lfs merge=lfs -text
*.png                 filter=lfs diff=lfs merge=lfs -text
*.jpg                 filter=lfs diff=lfs merge=lfs -text
*.zip                 filter=lfs diff=lfs merge=lfs -text
# Compiled Go binaries (if you commit them)
lib/go/bin/*          filter=lfs diff=lfs merge=lfs -text

# Normalize line endings for text so Windows/Unix don't fight
*.sh   text eol=lf
*.py   text eol=lf
*.yaml text eol=lf
*.md   text eol=lf
