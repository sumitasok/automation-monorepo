# Worklog — 2026-07-04

- Set up the automation-monorepo: structure, manifest system, `auto` CLI, plan doc.

<!-- `auto log "..."` appends dated entries here. One file per day. -->
- 12:19 — restructured into framework + packs (parent/pack model); added visibility + doctor leak-guard
- 13:03 — data store: state DBs now local-only (git-ignored); config stays versioned; ADR 0005
- 13:15 — added app-backed jobs (exec/workdir); ADR 0006 apps-as-packs + gmail case study
